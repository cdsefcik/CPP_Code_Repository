#include<iostream>
#include<vector>
using namespace std;
/*
int main() {
	vector<int>* vp = new vector<int>(); //Dynamic
	vector<int> v; //Static
	vector<int> v2;
	vector<int> v3(5,2); //Creates a vector of 5 elements, each with a value of 2.
	v.push_back(10);
	v.push_back(20);
	//v.pop_back();
	v.push_back(30);
	

	cout << v[0] << endl;
	cout << v[1] << endl;
	cout << v[2] << endl;

	v.push_back(23);
	v.push_back(234);

	cout << v.at(3) << endl; //Use the at function, checks the index. if use square bracktest, then you could get garbage values.
	for (int i = 0; i < v.size(); i++) {
		cout << v[i] << endl; //Can use both at or []
	}

	for (int i = 0; i < 100; i++) {
		cout << "Capacity" << v2.capacity() << endl;
		v2.push_back(i+1);
		cout << "Size" << v2.size() << endl;
	}

	for (int i = 0; i < v2.size(); i++) {
		cout << v2[i] << endl;
	}
}
*/